# API base URL in dev (production is same-origin, leave empty)
VITE_API_URL=http://localhost:3001
VITE_SENTRY_DSN=
# NOTE: no VITE_SUPABASE_* here on purpose - the storefront ships zero
# supabase-js (150KB bundle budget); all data goes through /v1/public.
